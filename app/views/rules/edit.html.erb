<%= link_to t("rules.back_to_rules"), rules_path %>

<%= render DS::Dialog.new do |dialog| %>
  <%
    title = if @rule.name.present?
              t("rules.edit.title_with_name", resource: t("rules.resources.#{@rule.resource_type}.one"), name: @rule.name)
            else
              t("rules.edit.title", resource: t("rules.resources.#{@rule.resource_type}.one"))
            end
  %>
  <% dialog.with_header(title: title) %>

  <% dialog.with_body do %>
    <%= render "rules/form", rule: @rule %>
  <% end %>
<% end %>
