---
en:
  activerecord:
    attributes:
      import:
        col_sep: Column separator
        currency: Currency
        number_format: Number Format
        signage_convention: Amount type
    errors:
      models:
        import:
          attributes:
            raw_file_str:
              invalid_csv_format: is not a valid CSV format
